"!important" beside selector in .css gives that styling high priority
general priority: inline > id > class

outline:
css box model:
content -> padding -> margin -> border -> outline
outline-offset : gives padding between border and outline
outline has same shorthand and -stuff like border

text formatting:
text-align
text-align-last: alignment of last line in text
direction: rtl
unicode-bidi: bidi-override (mandatory for above  thing to work)
vertical-align: baseline, text-top,text-bottom,sub, super
text-top, text-bottom : on top/bottom of parent's font 
to see this on same size parent and child do {vertical-align: text-bottom; font-size: 15px}

text-decoration-overline underline line-through
text-decoration-color
text-decoration-thickness

<div class=bor></div>
.bor{
    border: 1px solid black
}
=> gives hr

<input style="border-left: none ly for top and right"=> you get a neat one line
but box comes when hovering, to avoid that: do outline left right none too

text-transform: uppercase / lowercase / capitalize
text-indent: 10px (spacing at the start)
letter-spacing: 2px (spacing between letters)
line-height: 1.0 (space between two lines)
word-spacing: 10px (space between words)
white-space: wrap {wraps into the element boundary}/ no wrap {it covers whole text in a single line, increasing the scroll bar, overflowing the container}(line change)

text-shadow: 1px(text suze) 1px(shadow suze) red, gives a shadow effect of red
(for blur add another pixel): 1px 1px 3px(blur effect) red
check w3 for a cool tricolor blur effect

font-style: normal/italic/oblique
font-weight: 100 to 900,bold,bolder,lighter
font-size: 10px (use px)
font-variant: small-caps (small letters turned to caps but will be in same size as small)

look up on how to import google fonts using <link>
always give border to divs for better idea

display: block / inline 
check w3 schools on display

visibility: hidden (hide the content but wont remove the space)
display: none (no space either here)

say you want 2 divs side by side:
div1 declare small width, div2 as inline but div1 takes up whole line despite small width. width and height defines area for content but the container remains

you cant declare height in inline, so to declare height in inline you go for inline block

maxwidth: width can be less than this

position: static (by default, only margin, no parent)
position: relative (checks from parent)
position: fixed (fixed even if you scroll (chatbot at te corner))
position: absolute (fixed a position according to a relative, image inside an image)
top, bottom, left right : 1 px (these check dist from parent ele while margin checks from border)

code review:
use float-right for categories and try it for the lists below too